{
  "name": "cinerate",
  "version": "1.0.0",
  "private": true,
  "description": "CineRate — full-stack movie rating site (deployed as one service)",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
